def fib(n: i32) -> i32:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main() -> i32:
    let i: i32 = 0
    while i <= 15:
        print("fib(")
        print(i)
        print(") = ")
        print(fib(i))
        print("\n")
        i = i + 1
    return 0